<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{user.real_name}}'s Articles</title>
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>

<header class="site-header">
    <div class="container">
        <div class="nav-left">
            <img src="{{user.avatar_url}}" alt="User Avatar" class="user-avatar">
            <p class="user-bio">{{user.bio}}</p>
        </div>

        <div class="nav-center">
            <h1>{{user.real_name}}'s Articles</h1>
        </div>

        <div class="nav-right">
            <a href="/edit-account/{{user.username}}">Edit myAccount</a>
            <a href="/logout">Logout</a>
        </div>
    </div>
</header>

<div class="container">
    <section class="user-posts">
        <h2>{{user.real_name}}'s Articles</h2>
        <a href="/new-article" class="new-article-btn">Write New Article</a>

        {{#if posts.length}}
            <ul class="posts-list">
                {{#each posts}}
                    <li class="post" data-article-id="{{this.id}}">
                        <h3>{{this.title}}</h3>
                        <p>{{this.content}}</p><!-- 修改为显示文章内容或摘要 -->
                        {{#if this.image_url}} <!-- 检查是否有图片URL -->
                            <div class="post-image">
                                <img src="/images{{this.image_url}}" alt="{{this.title}}" />
                            </div>
                        {{/if}}
                        <!-- 评论显示区 -->
                        <div class="comments-container" data-article-id="{{this.id}}">
                            <!-- 评论内容将通过JavaScript动态加载 -->
                        </div>

                            <!-- 发表评论表单 -->
                            <div class="comment-form-container">
                                <form class="comment-form" data-article-id="{{this.id}}">
                                    <textarea name="comment" placeholder="Leave a comment..."></textarea>
                                    <button type="submit">Submit</button>
                                </form>
                            </div>

                        <div class="post-actions">
                            <a href="#" class="delete-article-btn" data-article-id="{{this.id}}">Delete</a>
                        </div>
                    </li>
                {{/each}}
            </ul>
        {{else}}
            <p>You have not written any articles yet.</p>
        {{/if}}
    </section>
</div>

<footer class="site-footer">
    <div class="container">
        &copy; {{currentYear}} {{blogTitle}}. All rights reserved.
    </div>
</footer>

<script src="/js/my-articles.js"></script>
</body>
</html>
